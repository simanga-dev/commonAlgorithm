/// <summary>
/// Valid Anagram (Dictionary)
/// 
/// Given two strings s and t, return true if t is an anagram of s, and false otherwise.
/// An anagram uses all the original letters exactly once (rearrangement).
/// 
/// Examples:
///   s = "anagram", t = "nagaram" → true
///   s = "rat",     t = "car"     → false
/// </summary>
public static class ValidAnagramDictionary
{
    public static bool IsAnagram(string s, string t)
    {
        // TODO: Implement using a Dictionary<char, int> for character frequency counting.
        // Hint: Count characters in s, then decrement for each character in t — all counts should be zero.
        throw new NotImplementedException();
    }
}
