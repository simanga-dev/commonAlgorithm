public static class LongestConsecutiveSequenceHashset
{
    /// <summary>
    /// Given an unsorted array of integers nums, return the length of the longest
    /// consecutive elements sequence. The algorithm must run in O(n) time.
    ///
    /// Example: nums = [100,4,200,1,3,2] → 4 (the sequence [1,2,3,4])
    /// </summary>
    public static int LongestConsecutive(int[] nums)
    {
        // TODO: Implement using a HashSet.
        // Hint: Insert all numbers into a HashSet, then only start counting from
        //       numbers that have no predecessor (num-1 not in set).
        throw new NotImplementedException();
    }
}
