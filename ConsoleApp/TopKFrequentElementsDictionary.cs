public static class TopKFrequentElementsDictionary
{
    /// <summary>
    /// Given an integer array nums and an integer k, return the k most frequent
    /// elements. You may return the answer in any order.
    ///
    /// Example: nums = [1,1,1,2,2,3], k = 2 → [1,2]
    /// Example: nums = [1], k = 1 → [1]
    /// </summary>
    public static int[] TopKFrequent(int[] nums, int k)
    {
        // TODO: Implement using a Dictionary to count frequencies.
        // Hint: Count frequencies with a Dictionary, then sort entries by
        //       frequency descending and take the top k keys.
        throw new NotImplementedException();
    }
}
