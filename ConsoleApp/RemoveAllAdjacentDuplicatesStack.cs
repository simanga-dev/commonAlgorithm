/// <summary>
/// Remove All Adjacent Duplicates (Stack)
/// 
/// Given a string s, repeatedly remove adjacent duplicate characters until no
/// more adjacent duplicates exist. Return the final string.
/// 
/// Examples:
///   s = "abbaca" → "ca"   (remove "bb" → "aaca", remove "aa" → "ca")
///   s = "azxxzy" → "ay"   (remove "xx" → "azzy", remove "zz" → "ay")
/// </summary>
public static class RemoveAllAdjacentDuplicatesStack
{
    public static string RemoveDuplicates(string s)
    {
        // TODO: Implement using a Stack<char> to track characters.
        // Hint: Push each character; if it matches the top of the stack, pop instead.
        throw new NotImplementedException();
    }
}
